json.extract! user, :role, :first_name, :last_name, :nickname, :email, :encrypted_password, :reset_password_token, :birthday, :geneder, :is_deleted, :is_blocked, :phone
json.url user_url(user, format: :json)