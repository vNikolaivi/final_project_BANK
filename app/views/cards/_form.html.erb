<%= form_with(model: card, local: true) do |form| %>
  <% if card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% card.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :card_number %>
    <%= form.text_field :card_number %>
  </div>

  <div class="field">
    <%= form.label :security_type %>
    <%= form.text_field :security_type %>
  </div>

  <div class="field">
    <%= form.label :security_code %>
    <%= form.text_field :security_code %>
  </div>

  <div class="field">
    <%= form.label :expires_end %>
    <%= form.text_field :expires_end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>