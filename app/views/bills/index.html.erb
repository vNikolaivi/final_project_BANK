<p id="notice"><%= notice %></p>

<h2>Bills Information</h2>

<table>
  <thead>
  <tr>
    <th>| # |</th>
    <th>&nbsp;User Name&nbsp; |</th>
    <th>&nbsp;Bill Type  |</th>
    <th>&nbsp;Balance  |</th>
    <th>&nbsp;Bill Status  |</th>
    <th>&nbsp;Created Date  |</th>
    <th colspan="3">Operations</th>
  </tr>
  </thead>

  <tbody>
  <% @bill_info.each do |bill_info| %>
    <tr>
      <td><%= bill_info.id %>&nbsp;</td>
      <td><%= bill_info.username %>&nbsp;</td>
      <td>&nbsp;<%= bill_info.bill_type %>&nbsp;</td>
      <td>&nbsp;<%= bill_info.amount %>&nbsp;</td>
      <td>&nbsp;<%= bill_info.bill_status %>&nbsp;</td>
      <td>&nbsp;<%= bill_info.created_at %>&nbsp;</td>
      <td><%= link_to 'Show', bill %></td>
      <td><%= link_to 'Edit', edit_bills_info_path(bill) %></td>
      <td><%= link_to 'Delete', bill, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Create New Bill', new_bills_info_path %>